# graphify
- **graphify** (`~/.Codex/skills/graphify/SKILL.md`) - any input to knowledge graph. Trigger: `/graphify`
When the user types `/graphify`, invoke the Skill tool with `skill: "graphify"` before doing anything else.

# git workflow
- For this repository, use a dedicated feature branch with the prefix `feature/` for task-scoped commit/push work unless the user explicitly requests another branch or direct push to `main`.
- When a ClickUp task ID is available, include it in the branch name, for example `feature/86c9nwzta-workspaces`.
- Do not default to pushing task-scoped implementation directly to `main`.
